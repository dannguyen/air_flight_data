      <% head_fields = [ :year, :month, :arrivals, :delayed_arrivals_rate, :carrier_delayed_arrivals_rate, 
         :weather_delayed_arrivals_rate, :nas_delayed_arrivals_rate, :late_aircraft_delayed_arrivals_rate, :security_delayed_arrivals_rate ]

         head_fields = extra_fields + head_fields if defined?extra_fields
          %>


<table class="table table-striped table-bordered">
   <thead><tr>      
      <% head_fields.each do |hf| %>
         <%= stat_to_th( hf) %>
      <% end %>
   </tr></thead>
   <tbody>
      <% records.each do |record| %>
      <tr>
         <% head_fields.each do |hf| %>
            <%= stat_to_td(record, hf) %>
         <% end %>         
      </tr>
      <% end %>
      
   </tbody>
</table>